package com.project.mapper;

import java.util.List;

import org.apache.ibatis.annotations.Mapper;
import org.apache.ibatis.annotations.Param;

import com.project.model.krhBoardVO;

@Mapper
public interface krhBoardMapper {
	//페이징 처리된 전체 게시글 수 조회 + 검색
	int countBoard(@Param("findStr")String findStr);
	
	//페이징된 목록 조회 + 검색
	List<krhBoardVO>getBoardList(@Param("startRow") int startRow, @Param("pageSize") int pageSize, @Param("findStr") String findStr);
}
