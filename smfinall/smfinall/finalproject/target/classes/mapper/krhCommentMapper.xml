<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.mapper.krhCommentMapper">

	<!--요리 고민방 댓글 목록 조회-->
	<select id="commentList" parameterType="int" resultType="com.project.model.krhCommentVO">
		select * from comment where boardId=#{boardId} and replyId=0 order by createdAt asc
	</select>
	
	<!--요리 고민방 대댓글 목록 조회-->
	<select id="commentListReply" parameterType="int" resultType="com.project.model.krhCommentVO"> 
		select * from comment where replyId=#{commentId} order by createdAt asc
	</select>

	<!--요리 고민방 댓글 추가 -->
	<insert id="addComment" parameterType="map">
		insert into comment(boardId, author, authorId, content, replyId, createdAt) values(#{boardId}, #{author}, #{authorId}, #{content}, 0, now())
	</insert>
	
	<!--요리 고민방 대댓글 추가 -->
	<insert id="addReply" parameterType="map">
		insert into comment(author, authorId, content, replyId, createdAt) values(#{author}, #{authorId}, #{content}, #{replyId}, now())
	</insert>
	
	<!--요리 고민방 댓글 수정 -->
	<update id="updateComment" parameterType="map">
	    update comment
	    set content = #{content}, updatedAt = now()
	    where commentId = #{commentId}
	</update>
	
	<!--요리 고민방 댓글 삭제 -->
	<delete id="deleteComment" parameterType="int">
		delete from comment WHERE commentId = #{commentId}
	</delete>
	
</mapper>